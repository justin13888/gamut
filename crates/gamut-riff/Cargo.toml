[package]
name = "gamut-riff"
description = "The WebP RIFF container: chunk read/write, the VP8X feature header, and spec-checked still-image layout."
version = "1.0.0"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
gamut-core.workspace = true

[dev-dependencies]
# libwebp's demuxer as the differential container oracle (FFI). Dev-only: the shipped library is
# C-free and `#![forbid(unsafe_code)]`.
libwebp-sys2.workspace = true
divan.workspace = true

[[bench]]
name = "container"
harness = false
